# @package _global_

defaults:
  - _self_
  - /train/dqn: cartpole

seed: 0

load_model_path: ???
output: ./tau_estimates.npy

n_rollouts: 500
max_lag: 500
knn: 20
num_cores: 1

hydra:
  job:
    chdir: false
  run:
    dir: .
  output_subdir: null